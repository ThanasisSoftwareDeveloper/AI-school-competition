Goal: Train Decision Tree classifiers that can predict whether a student will pass the course based on the following characteristics.
* Weekly personal study hours (0 to 30 hours)
* Course attendance rate (0 to 1)
* Hours of sleep per day (4 to 10 hours)
* Assignment completion rate (0 to 1)
* Stress level (0 to 1)
For the Easy level, you only need to consider the first two features, while for the Hard level, you need to consider all the features provided. The decision tree must predict whether the student passed the course or not (0: failed and 1: passed).

What should we add to the code below that I’ll give you, 
at which point or points, and 
why?
 The given code and methodology are as follows:
1. We download the data:

from gdown import download
download(id='1sP-Pv9YpTiwz4EuvXszaqapsx7nYqvpO', output='easy_train_data.csv', quiet=False)
download(id='1TsC4bIXhqXP72hcG10f0rAL-TSPO6Nr1', output='hard_train_data.csv', quiet=False)
              

Ορίζουμε το μοντέλο προς εκπαίδευση:
import pandas as pd
from sklearn.tree import DecisionTreeClassifier
 trees = {}
for level in ['easy', 'hard']:
    df = pd.read_csv(f'{level}_train_data.csv')
    print('Επίπεδο', level)
    display(df)
    X = df.values[:,:-1]
    y = df.values[:,-1].astype(int)
    tree = DecisionTreeClassifier(max_depth=5, random_state=42) # TODO: Επιλέξτε κατάλληλες παραμέτρους(max_depth=5, random_state=42) 
    # TODO: Εκπαιδεύστε το δέντρο (tree.fit(X, y))
tree.fit(X, y)
    trees[level] = tree  

# Level easy
study_hours    attendance    pass
0    14.778753    0.930949    1
1    7.420443    0.681118    0
2    27.626172    0.669487    1
3    22.136908    0.336896    0
4    16.462014    0.426181    0

#800 rows × 3 columns
3. Αποθηκεύουμε τις απαντήσεις για υποβολή στο site:

def storeAnswers(trees):
    answers = {}
    for level in ['easy', 'hard']:
        clf = trees[level]
        try:
            answers[level] = {
                "children_left": clf.tree_.children_left.tolist(),
                "children_right": clf.tree_.children_right.tolist(),
                "feature": clf.tree_.feature.tolist(),
                "threshold": clf.tree_.threshold.tolist(),
                "label": clf.classes_[ clf.tree_.value[:,0].argmax(axis=-1) ].tolist()
            }
        except:
            print(f'{level}: Εκπαιδεύστε το δέντρο πριν το αποθηκεύσετε')

    with open('answers.json', 'w') as f:
        json.dump(answers, f)

storeAnswers(trees) # Κατεβάστε το αρχείο 'answers.json' για υποβολή στο site

from google.colab import files
files.download('answers.json')
